# Cleany Agent Instructions

This repository's agent-facing project context is maintained in the LLMWiki.

Start here:

```text
gdrive/Obsidian_Vault/llms.txt
```

When working in this repo:

1. Read `gdrive/Obsidian_Vault/llms.txt` first.
2. Follow the linked LLMWiki files in the order listed there.
3. Treat `gdrive/Obsidian_Vault/` as the source of synthesized project knowledge and agent memory.
4. Do not commit `gdrive/`; it is a local mount/symlink path and is ignored by git.
