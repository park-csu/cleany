#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"
ROS2_WS="${REPO_ROOT}/ros2_ws"

cd "${ROS2_WS}"
exec "${SCRIPT_DIR}/docker-exec-humble.sh" colcon "$@"
