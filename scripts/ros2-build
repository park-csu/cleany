#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/docker-exec-humble.sh" bash -lc 'cd /workspace/cleany/ros2_ws && exec colcon build --symlink-install "$@"' bash "$@"
